Performance history aggregation:
- 1h keeps raw samples.
- 24h groups samples into 15-minute UTC buckets.
- 7d groups samples into 1-hour UTC buckets.
- API limits are applied after aggregation so the selected period is represented across the chart.
